import { Button } from '../common/Button';
export function AddressInput({ value, onChange, onAnalyze, loading }: { value: string; onChange: (value: string) => void; onAnalyze: (value: string) => void; loading?: boolean }) { return <div className="rounded-2xl border border-slate-200 bg-white p-3 shadow-lg shadow-slate-200/40"><textarea value={value} onChange={(event) => onChange(event.target.value)} placeholder="Flat 403, Sai Residency, behind D-Mart, Hinjewadi, Pune" className="min-h-28 w-full resize-none p-3 text-base text-ink outline-none placeholder:text-slate-400" /><div className="flex justify-end border-t border-slate-100 pt-3"><Button disabled={loading || value.trim().length < 3} onClick={() => onAnalyze(value)}>{loading ? 'Analyzing…' : 'Analyze Address'}</Button></div></div>; }
