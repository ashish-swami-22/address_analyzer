import type { AddressIssue } from '@app/shared'; import { Badge } from '../common/Badge'; import { Card } from '../common/Card';
export function AddressIssues({ issues, recommendations }: { issues: AddressIssue[]; recommendations: string[] }) { return <Card><h3 className="font-semibold text-ink">Potential issues</h3>{issues.length ? <div className="mt-4 space-y-3">{issues.map((issue) => <div className="rounded-lg bg-amber-50 p-3" key={issue.title}><Badge tone="amber">{issue.severity}</Badge><p className="mt-2 text-sm font-medium text-amber-900">{issue.title}</p><p className="mt-1 text-sm text-amber-800">{issue.description}</p></div>)}</div> : <p className="mt-3 text-sm text-teal-700">No significant issues detected.</p>}<h3 className="mt-6 font-semibold text-ink">Recommendations</h3><ul className="mt-3 list-disc space-y-2 pl-5 text-sm text-slate-600">{recommendations.map((item) => <li key={item}>{item}</li>)}</ul></Card>; }
