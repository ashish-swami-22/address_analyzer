import { Card } from '../common/Card'; import { ScoreIndicator } from '../common/ScoreIndicator';
export function AddressScore({ completenessScore, locationConfidence }: { completenessScore: number; locationConfidence: number | null }) { return <Card><h3 className="mb-5 font-semibold text-ink">Address resolution scores</h3><div className="space-y-5"><ScoreIndicator label="Address completeness" score={completenessScore} />{locationConfidence === null ? <p className="text-sm text-slate-500">Location confidence is not available until a reliable location can be resolved.</p> : <ScoreIndicator label="Location confidence" score={locationConfidence} />}</div></Card>; }
