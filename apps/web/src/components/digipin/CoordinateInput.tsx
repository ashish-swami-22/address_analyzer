import { useState } from 'react'; import { Button } from '../common/Button'; import { Card } from '../common/Card';
export function CoordinateInput({ onSubmit, result }: { onSubmit: (latitude: number, longitude: number) => void; result?: string }) { const [latitude, setLatitude] = useState('28.622788'); const [longitude, setLongitude] = useState('77.213033'); return <Card><h3 className="font-semibold text-ink">Coordinates → DIGIPIN</h3><div className="mt-4 grid grid-cols-2 gap-3"><input value={latitude} onChange={(e) => setLatitude(e.target.value)} className="rounded-lg border border-slate-200 p-3" placeholder="Latitude" /><input value={longitude} onChange={(e) => setLongitude(e.target.value)} className="rounded-lg border border-slate-200 p-3" placeholder="Longitude" /></div><Button className="mt-3" onClick={() => onSubmit(Number(latitude), Number(longitude))}>Generate DIGIPIN</Button>{result && <p className="mt-4 font-mono text-lg text-teal">{result}</p>}</Card>; }
