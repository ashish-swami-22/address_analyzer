import { useState } from 'react'; import { Button } from '../common/Button'; import { Card } from '../common/Card';
export function DigipinInput({ onSubmit, result }: { onSubmit: (value: string) => void; result?: string }) { const [value, setValue] = useState('39J-49L-L8T4'); return <Card><h3 className="font-semibold text-ink">DIGIPIN → coordinates</h3><input value={value} onChange={(e) => setValue(e.target.value)} className="mt-4 w-full rounded-lg border border-slate-200 p-3 font-mono" /><Button className="mt-3" onClick={() => onSubmit(value)}>Find coordinates</Button>{result && <p className="mt-4 text-sm text-slate-600">{result}</p>}</Card>; }
