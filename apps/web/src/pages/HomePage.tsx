import { useState } from 'react'; import { useMutation } from '@tanstack/react-query'; import { AddressInput } from '../components/address/AddressInput'; import { AnalysisResult } from '../components/address/AnalysisResult'; import { Card } from '../components/common/Card'; import { analyzeAddress } from '../services/api';
const examples = [['Complete address', 'Flat 403, Sai Residency, behind D-Mart, Hinjewadi, Pune'], ['Missing building', 'Flat 4, Main Road, Jaipur'], ['Landmark only', 'Near the temple, Sector 12, Noida'], ['Misspelled locality', 'Hinjewadi, Puney, Maharashtra'], ['Incorrect PIN', 'MG Road, Bengaluru 400001'], ['Hinglish address', 'D-Mart ke piche, Sai Residency, Pune']];
export function HomePage() { const mutation = useMutation({ mutationFn: analyzeAddress }); const [address, setAddress] = useState(''); const [hasSubmitted, setHasSubmitted] = useState(false); const analyze = (value: string) => { setAddress(value); setHasSubmitted(true); mutation.mutate(value); }; return <main><section className="border-b border-slate-200 bg-sand"><div className="mx-auto max-w-6xl px-5 py-20"><p className="mb-4 text-sm font-semibold uppercase tracking-widest text-teal">Geospatial address intelligence</p><h1 className="max-w-3xl text-4xl font-bold tracking-tight text-ink sm:text-6xl">India Address Intelligence</h1><p className="mt-5 max-w-2xl text-lg leading-8 text-slate-600">Understand, validate and locate Indian addresses with geospatial intelligence and reliable location resolution.</p><div className="mt-10 max-w-3xl"><AddressInput value={address} onChange={setAddress} loading={mutation.isPending} onAnalyze={analyze} /></div></div></section><section className="mx-auto max-w-6xl px-5 py-12">{mutation.isPending ? <div className="rounded-2xl border border-slate-200 bg-white p-12 text-center text-slate-500">Analyzing address…</div> : mutation.error ? <div className="rounded-2xl border border-red-200 bg-red-50 p-6 text-red-800"><p className="font-semibold">Unable to load the analysis</p><p className="mt-2 text-sm">Make sure the API is running at <code className="rounded bg-red-100 px-1">http://localhost:4000</code>, then try again.</p></div> : hasSubmitted && mutation.data ? <AnalysisResult result={mutation.data} /> : <div className="rounded-2xl border border-dashed border-slate-300 p-12 text-center text-slate-500">Enter an address above to see completeness, location confidence, parsed components and resolution details.</div>}<div className="mt-14"><div className="mb-5"><p className="text-sm font-semibold uppercase tracking-widest text-teal">Try an example</p><h2 className="mt-2 text-2xl font-bold text-ink">See how different address conditions resolve</h2></div><div className="grid gap-4 md:grid-cols-2 lg:grid-cols-3">{examples.map(([title, example]) => <Card key={title} className="flex flex-col"><h3 className="font-semibold capitalize text-ink">{title}</h3><p className="mt-3 flex-1 text-sm leading-6 text-slate-600">{example}</p><button onClick={() => analyze(example)} className="mt-5 self-start text-sm font-semibold text-teal hover:text-ink">Use this example →</button></Card>)}</div></div></section></main>; }
