import { useState } from 'react'; import { AddressInput } from '../components/address/AddressInput'; import { Card } from '../components/common/Card';
const samples = [['Complete Address','Flat 403, Sai Residency, Hinjewadi, Pune'],['Missing Building','Flat 4, Main Road, Jaipur'],['Landmark Only','Near the temple, Sector 12, Noida'],['Misspelled Locality','Hinjewadi, Puney, Maharashtra'],['Incorrect PIN','MG Road, Bengaluru 400001'],['Hinglish Address','D-Mart ke piche, Sai Residency, Pune']];
export function AddressLabPage() { const [selected, setSelected] = useState(''); return <main className="mx-auto max-w-6xl px-5 py-12"><p className="text-sm font-semibold uppercase tracking-widest text-teal">Development utility</p><h1 className="mt-3 text-4xl font-bold text-ink">Address Lab</h1><p className="mt-4 text-slate-600">This route is retained for development. The primary examples now live on the Home page.</p><div className="mt-10 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{samples.map(([title, example]) => <Card key={title}><h2 className="font-semibold text-ink">{title}</h2><p className="mt-3 min-h-12 text-sm leading-6 text-slate-600">{example}</p><button onClick={() => setSelected(example)} className="mt-5 text-sm font-semibold text-teal hover:text-ink">Use sample →</button></Card>)}</div>{selected && <div className="mt-10 max-w-3xl"><AddressInput value={selected} onChange={setSelected} onAnalyze={() => undefined} /><p className="mt-3 text-sm text-slate-500">Development utility only.</p></div>}</main>; }
